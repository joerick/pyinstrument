[aliases]
test = pytest

[tool:pytest]
testpaths =
    test
    courseProjectDocs
addopts = --cov=. --cov-report=term-missing --cov-report=html

[mutmut]
runner = pytest
paths_to_mutate =
    pyinstrument
tests_dir =
    test
    courseProjectDocs

mutate_only_covered_lines = true
debug = true
